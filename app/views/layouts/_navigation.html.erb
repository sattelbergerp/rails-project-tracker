<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%=root_path%>">Project Tracker</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if current_user!=nil %>
          <%= header_link("Projects", projects_path) %>
          <li class="dropdown <%= 'active' if current_page?(tasks_path) || current_page?(overdue_tasks_path)%>">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Tasks <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <%= header_link("Overdue", overdue_tasks_path) %>
              <%= header_link("All", tasks_path) %>
            </ul>
          </li>
          <%= header_link("New Project", new_project_path) %>
          <%= header_link("New Task", new_task_path) %>
        <% else %>
          <%= header_link("Login", new_user_session_path) %>
          <%= header_link("Signup", new_user_registration_path) %>
          <%= header_link("Login with Github", user_github_omniauth_authorize_path) %>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user!=nil %>
          <li><%= link_to "Logout #{current_user.email}", destroy_user_session_path, method: 'delete'%></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
